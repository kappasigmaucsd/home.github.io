import styled from "styled-components";

export const StyledTimer = styled.div`
  background-color: var(--blue);
  border-radius: 50px;
  padding: 10px 25px;

  font-size: 2rem;

  margin: 15px;

  p {
    margin: 0 15px;
  }
`;
